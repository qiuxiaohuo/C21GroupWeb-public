# C21GroupWeb Public

这是 Mechanics 知识文档的公开发布仓库，使用 MkDocs Material 构建。

## 本地预览

```shell
python -m pip install -r requirements.txt
mkdocs serve
```

## 发布

推送到 `main` 分支后，GitHub Actions 会自动构建并发布 GitHub Pages。

