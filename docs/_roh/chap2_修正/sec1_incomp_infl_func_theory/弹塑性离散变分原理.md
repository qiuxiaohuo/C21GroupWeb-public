# 弹塑性离散变分原理

本文档通过整理 Simo 和 Hughes (1997) 第四章的内容，旨在理解有限元计算的假设应变方法。

## 增量变分原理

这一部分将给出弹塑性方程的增量变分原理，由该变分原理可得到全部的平衡方程、几何方程、本构方程和塑性加卸载条件。将变形体 $B$ 在时刻 $t\in[0,\, T]$ 的**总自由能** $\mathscr{P}_{t}$ 表示为多场变分形式：
$$
\begin{equation}\label{eq:hu_washizu}
\begin{aligned}
&\mathscr{P}_{t}(\boldsymbol{u}_{t},\, \boldsymbol{\varepsilon}_{t}-\boldsymbol{\varepsilon}_{t}^{p},\, 
\boldsymbol{q}_{t},\, \boldsymbol{\sigma}_{t}) \\
= &\int_{B} \big[ \Psi(\boldsymbol{\varepsilon}_{t}-\boldsymbol{\varepsilon}_{t}^{p},\, \boldsymbol{q}_{t})
+ \boldsymbol{\sigma}_{t}:( \nabla_{s}\boldsymbol{u}_{t} - \boldsymbol{\varepsilon}_{t} ) \big] \,\mathrm{d}V
+ \mathscr{P}_{\mathrm{ext}}(\boldsymbol{u}_{t}),
\end{aligned}
\end{equation}
$$

式中，$\square_{t}$ 表示与时间相关的场变量。$\boldsymbol{u}_{t}\in H_{1}(B)$ 是位移场，$\boldsymbol{\varepsilon}_{t},\, \boldsymbol{\varepsilon}_{t}^{p},\, \boldsymbol{\sigma}_{t}\in L_{2}(B)$ 分别是应变、塑性应变和应力场，$\boldsymbol{q}_{t}\in L_{2}(B)$ 是状态变量场。注意这些场变量的函数空间与场变量取值的向量或张量维度匹配。$\Psi$ 由单位体积的应变能 $W$ 和考虑硬化的二次型 $\boldsymbol{\mathsf{D}}$ 组成，

$$
\Psi_{t} = W(\boldsymbol{\varepsilon}- \boldsymbol{\varepsilon}^{p})
+ \frac{1}{2} \boldsymbol{q} \cdot \boldsymbol{\mathsf{D}}^{-1} \cdot\boldsymbol{q},
$$

而在考虑准静态加载时，也即体积外力 $\boldsymbol{b}$ 和作用在自然边界 $\partial_{\boldsymbol{\sigma}}B$ 的面力 $\boldsymbol{t}$ 不随时间变化，外力势能等于

$$
\mathscr{P}_{\mathrm{ext}}(\boldsymbol{u}_{t})
= -\int_{B}\rho\boldsymbol{b} \cdot\boldsymbol{u}_{t}\,\mathrm{d}V
- \int_{\partial_{\boldsymbol{\sigma}}B} \bar{\boldsymbol{t}} \cdot \boldsymbol{u}_{t}\,\mathrm{d}\Gamma.
$$

而在时刻 $\xi\in[0,T]$ 的单位体积材料的塑性耗散 $\mathscr{D}_{\xi}^{p}$ 在应变空间 $(\boldsymbol{\varepsilon}_{\xi},\, \boldsymbol{\varepsilon}_{\xi}^{p},\, \boldsymbol{q}_{\xi}) \in \mathcal{E}_{\boldsymbol{\varepsilon}}$ 表示为

$$
\mathscr{D}_{\xi}^{p}
= \dot{\boldsymbol{\varepsilon}}_{\xi}^{p} : \nabla W(\boldsymbol{\varepsilon}_{\xi}-\boldsymbol{\varepsilon}_{\xi}^{p}) - \dot{\boldsymbol{q}}_{\xi} \cdot \boldsymbol{\mathsf{D}}^{-1}\cdot\boldsymbol{q}_{\xi},
$$

式中的 $\mathbb{E}_{\boldsymbol{\varepsilon}}$ 描述的是材料容许的状态空间，或者称弹性区域。在区域 $B$ 和时间区间 $[0,\, t]$ 内积分，得到总塑性耗散能 $\mathscr{L}_{t}^{p}$ 为

$$
\mathscr{L}_{t}^{p}
= \int_{0}^{t} \int_{B}
\big( \mathscr{D}_{\xi}^{p} - \gamma_{\xi} f[ \nabla W(\boldsymbol{\varepsilon}_{\xi}-\boldsymbol{\varepsilon}_{\xi}^{p}),\, \boldsymbol{q}_{\xi} ] \big)
\,\mathrm{d}V \,\mathrm{d}\xi,
$$

式中，$\gamma_{\xi}$ 解释为在时刻 $\xi$ 标量值的塑性乘子场。接下来将对泛函 $\mathscr{P}_{t}$ 和 $\mathscr{L}_{t}^{p}$ 进行时间域离散。记 $t_{n}$ 和 $t_{n+1}$ 时刻的物理量分别为 $\square_{n}$ 和 $\square_{n+1}$，定义泛函 $\hat{\mathscr{P}}_{n}$ 为 $t_{n+1}$ 时刻的总自由能 $\mathscr{P}_{n+1}$，和 $[t_{n},\, t_{n+1}]$ 内的总塑性耗散 $\Delta\mathscr{L}_{n+1}^{p}$ 之和：

$$
\begin{equation}\label{eq:inc_variational}
\hat{\mathscr{P}}_{n} = \mathscr{P}_{n+1} + \Delta\mathscr{L}_{n+1}^{p}.
\end{equation}
$$

如果对 $\Delta\mathscr{L}_{n+1}^{p}$ 这一项采用向后欧拉积分方法，也即

$$
\begin{equation}\label{eq:backward_euler}
\Delta\mathscr{L}_{n+1}^{p}
= \int_{B}
\left\{
\begin{aligned}
&(\boldsymbol{\varepsilon}_{n+1}^{p}-\boldsymbol{\varepsilon}_{n}^{p}) : \nabla W(\boldsymbol{\varepsilon}_{n+1}-\boldsymbol{\varepsilon}_{n+1}^{p}) 
- (\boldsymbol{q}_{n+1}-\boldsymbol{q}_{n}) \cdot \boldsymbol{\mathsf{D}}^{-1}\cdot\boldsymbol{q}_{n+1}\\
-& \Delta\gamma f[ \nabla W(\boldsymbol{\varepsilon}_{n+1}-\boldsymbol{\varepsilon}_{n+1}^{p}),\, \boldsymbol{q}_{n+1} ]
\end{aligned}
\right\}
\,\mathrm{d}V,
\end{equation}
$$

式中使用 $\Delta\gamma$ 代替 $\Delta t \gamma_{n+1}$ 是暗示当前处理的塑性问题是**率无关**的。将式 $\eqref{eq:backward_euler}$ 和 $\eqref{eq:hu_washizu}$ 代入到定义的增量泛函式 $\eqref{eq:inc_variational}$ 中得到

$$
\begin{equation}\label{eq:inc_vari}
\begin{aligned}
&\hat{\mathscr{P}}_{n}(\overbrace{\boldsymbol{u}_{n+1},\, \boldsymbol{\varepsilon}_{n+1},\, \boldsymbol{\sigma}_{n+1},\, \boldsymbol{\varepsilon}_{n+1}^{p},\, \boldsymbol{q}_{n+1},\, \Delta\gamma}^{\triangleq\boldsymbol{\chi}_{n+1}}) \\
=& \int_{B} 
\left\{\begin{aligned}
&W(\boldsymbol{\varepsilon}_{n+1}- \boldsymbol{\varepsilon}_{n+1}^{p})
+ \tfrac{1}{2} \boldsymbol{q}_{n+1} \cdot \boldsymbol{\mathsf{D}}^{-1} \cdot\boldsymbol{q}_{n+1}
+ \boldsymbol{\sigma}_{n+1}:( \nabla_{s}\boldsymbol{u}_{n+1} - \boldsymbol{\varepsilon}_{n+1} )\\
+& (\boldsymbol{\varepsilon}_{n+1}^{p}-\boldsymbol{\varepsilon}_{n}^{p}) : \nabla W(\boldsymbol{\varepsilon}_{n+1}-\boldsymbol{\varepsilon}_{n+1}^{p}) 
- (\boldsymbol{q}_{n+1}-\boldsymbol{q}_{n}) \cdot \boldsymbol{\mathsf{D}}^{-1}\cdot\boldsymbol{q}_{n+1}\\
-& \Delta\gamma f[ \nabla W(\boldsymbol{\varepsilon}_{n+1}-\boldsymbol{\varepsilon}_{n+1}^{p}),\, \boldsymbol{q}_{n+1} ]
\end{aligned}\right\}
\,\mathrm{d}V \\
+& \mathscr{P}_{\mathrm{ext}}(\boldsymbol{u}_{n+1}),
\end{aligned}
\end{equation}
$$

其中 $t_{n}$ 时刻的场变量 $(\boldsymbol{\varepsilon}_{n}^{p},\, \boldsymbol{q}_{n})$ 视作已知。在对场变量 $\boldsymbol{\chi}_{n+1}$ 的容许空间 $\mathcal{V}$ 作与边界条件适定的约束之后，上述泛函取驻值的条件是对任意 $\delta\boldsymbol{\chi}\in\mathcal{V}$，在该方向的 Gateux 导数 $\delta\hat{\mathscr{P}}(\boldsymbol{\chi}_{n+1};\,\delta \boldsymbol{\chi})$ 总等于零，也即（公式使用 Simo 和 Hughes (1997) P. 165 编号）：

$$
\begin{align}
\delta\hat{\mathscr{P}}(\boldsymbol{\chi}_{n+1};\,\delta \boldsymbol{u}) = 0,\,\Rightarrow\,&
\phantom{-}\int_{B}\big[ \boldsymbol{\sigma}_{n+1}:\nabla_{s}\delta\boldsymbol{u} -\rho\boldsymbol{b} \cdot\delta\boldsymbol{u} \big]\,\mathrm{d}V
- \int_{\partial_{\boldsymbol{\sigma}}B} \bar{\boldsymbol{t}} \cdot \delta\boldsymbol{u} \,\mathrm{d}\Gamma
=0,\tag{4.2.14a}\label{subeq:du}\\
\delta\hat{\mathscr{P}}(\boldsymbol{\chi}_{n+1};\,\delta \boldsymbol{\sigma}) = 0,\,\Rightarrow\,&
\phantom{-}\int_{B}\big[ \delta\boldsymbol{\sigma}:(\nabla_{s}\boldsymbol{u}_{n+1}-\boldsymbol{\varepsilon}_{n+1} ) \big]\,\mathrm{d}V
=0,\tag{4.2.14b}\label{subeq:ds}\\
\delta\hat{\mathscr{P}}(\boldsymbol{\chi}_{n+1};\,\delta \boldsymbol{\varepsilon}) = 0,\,\Rightarrow\,&
\phantom{-}\int_{B}\big[ \nabla W_{n+1}  - \boldsymbol{\sigma}_{n+1}
+ (\boldsymbol{\varepsilon}_{n+1}^{p}-\boldsymbol{\varepsilon}_{n}^{p}
-\Delta\gamma\partial_{\boldsymbol{\sigma}}f_{n+1}) : \mathbb{L}_{n+1}\big]:\delta\boldsymbol{\varepsilon} \,\mathrm{d}V
=0,\tag{4.2.14c}\label{subeq:de}\\
\delta\hat{\mathscr{P}}(\boldsymbol{\chi}_{n+1};\,\delta \boldsymbol{\varepsilon}^{p}) = 0,\,\Rightarrow\,&
-\int_{B}(\boldsymbol{\varepsilon}_{n+1}^{p}-\boldsymbol{\varepsilon}_{n}^{p}
-\Delta\gamma\partial_{\boldsymbol{\sigma}}f_{n+1}) : \mathbb{L}_{n+1}:\delta\boldsymbol{\varepsilon} \,\mathrm{d}V
=0,\tag{4.2.14d}\label{subeq:dp}\\
\delta\hat{\mathscr{P}}(\boldsymbol{\chi}_{n+1};\,\delta \boldsymbol{q}) = 0,\,\Rightarrow\,&
-\int_{B}\big[ (\boldsymbol{q}_{n+1} - \boldsymbol{q}_{n}) \cdot  \boldsymbol{\mathsf{D}}^{-1} 
+\Delta\gamma \partial_{\boldsymbol{q}}f_{n+1} \big]\cdot\delta\boldsymbol{q}
\,\mathrm{d}V
=0,\tag{4.2.14e}\label{subeq:dq}\\
\delta\hat{\mathscr{P}}(\boldsymbol{\chi}_{n+1};\,\delta \gamma) \geq 0,\,\Rightarrow\,&
-\int_{B} \delta\gamma f_{n+1}
\,\mathrm{d}V
\geq0,\tag{4.2.14f}\label{subeq:dl}\\
\end{align}
$$
注意到式 $\eqref{subeq:dl}$ 给出的是变分不等式，这是因为 $\delta\gamma$ 受约束 $\Delta\gamma + \delta\gamma$ 必须在区域 $B$ 内大于等于零，所以在 $\Delta \gamma > 0$ 时，必须有 $f_{n+1}=0$；而 $\Delta\gamma=0$ 时，$\delta\gamma\geq 0$，受不等式符号约束有 $f_{n+1}\leq 0$。

## 应力应变的插值

上一节给出的多场变分原理允许对应力和应变场使用独立的插值空间，而不是根据插值得到的位移场后处理得到。以下使用 Voigt 记法将应力和应变张量转换为列向量。应力和应变的有限元插值空间定义为
$$
\mathcal{V}_{\boldsymbol{\sigma}}^{h}
= \{ \boldsymbol{\sigma}^{h} \in \mathcal{V}_{\boldsymbol{\sigma}} 
\mid \boldsymbol{\sigma}^{h}|_{B^{e}}
=\boldsymbol{S}(\boldsymbol{x}) \boldsymbol{c}^{e}, \, 
\boldsymbol{c}^{e}\in \mathbb{R}^{m} \}, \\
\mathcal{V}_{\boldsymbol{\varepsilon}}^{h}
= \{ \boldsymbol{\varepsilon}^{h} \in \mathcal{V}_{\boldsymbol{\varepsilon}} 
\mid \boldsymbol{\varepsilon}^{h}|_{B^{e}}
=\boldsymbol{E}(\boldsymbol{x}) \boldsymbol{a}^{e}, \, 
\boldsymbol{a}^{e}\in \mathbb{R}^{m} \}.
$$
可以看到，单元之间应力和应变场通过 $\boldsymbol{c}^{e}$ 和 $a^{e}$ 独立进行插值，因此式 (4.2.14) 在逐个单元上成立。一方面，将上述应力应变场插值表达式代入到 $\eqref{subeq:ds}$，再由 $\boldsymbol{c}^{e}$ 的任意性，得到
$$
\int_{B^{e}}   \boldsymbol{S}^{\top}\nabla_{s}\boldsymbol{u}_{n+1}\,\mathrm{d}V
= \boldsymbol{H} \boldsymbol{a}_{n+1}^{e}, \quad
\underbrace{\boldsymbol{H}}_{m\times m} \triangleq \int_{B^{e}} \boldsymbol{S}^{\top}\boldsymbol{E}\,\mathrm{d}V.
$$
求解 $\boldsymbol{a}_{n+1}^{e}$ 后代入到应变场表达式中得到
$$
\boldsymbol{\varepsilon}_{n+1}|_{B^{e}}
= \bar{\nabla}^{s} \boldsymbol{u}_{n+1}
\triangleq \boldsymbol{E}(\boldsymbol{x})\boldsymbol{H}^{-1} \int_{B^{e}}  \boldsymbol{S}^{\top}\nabla_{s}\boldsymbol{u}_{n+1}\,\mathrm{d}V.
$$
另一方面，注意到式 $\eqref{subeq:de}$ 和 $\eqref{subeq:dp}$ 联立后得到
$$
\int_{B}\big( \nabla W_{n+1}  - \boldsymbol{\sigma}_{n+1} \big):\delta\boldsymbol{\varepsilon} \,\mathrm{d}V =0,
$$
将应力和应变场的插值表达式代入后，按照与应变场类似的求解方式，可得到
$$
\boldsymbol{\sigma}_{n+1}|_{B^{e}}
= \boldsymbol{S}(\boldsymbol{x})\boldsymbol{H}^{-\top} \int_{B^{e}}  \boldsymbol{E}^{\top} 
\nabla W( \boldsymbol{\varepsilon}_{n+1} - \boldsymbol{\varepsilon}_{n+1}^{p} )
\,\mathrm{d}V.
$$

$$
\int_{B^{e}} \boldsymbol{\sigma}_{n+1}:\nabla_{s}\delta\boldsymbol{u} \,\mathrm{d}V
- \int_{B^{e}} \rho\boldsymbol{b} \cdot\delta\boldsymbol{u} \,\mathrm{d}V
- \int_{\partial_{\boldsymbol{\sigma}}B^{e}} \bar{\boldsymbol{t}} \cdot \delta\boldsymbol{u} \,\mathrm{d}\Gamma
=0
$$

$$
\int_{B^{e}} \boldsymbol{\sigma}_{n+1}^{\top} \big( \nabla_{s}\delta\boldsymbol{u} \big) \,\mathrm{d}V
= \int_{B^{e}}  
\big[ \nabla W( \boldsymbol{\varepsilon}_{n+1} - \boldsymbol{\varepsilon}_{n+1}^{p} ) \big]^{\top}
\underbrace{\boldsymbol{E}
\boldsymbol{H}^{-1} 
\Big( \int_{B^{e}}
\boldsymbol{S}^{\top}
\big( \nabla_{s}\delta\boldsymbol{u} \big)
\,\mathrm{d}\boldsymbol{x} \Big) }_{=\bar{\nabla}_{s}\delta\boldsymbol{u}}
\,\mathrm{d}\boldsymbol{y}
$$


